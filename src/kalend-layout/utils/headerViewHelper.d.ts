import { CALENDAR_VIEW, Config } from '../index';
import { DateTime } from 'luxon';
export declare const calculatePositionForHeaderEvents: (events: any, width: number, calendarDays: DateTime[], config: Config, selectedView: CALENDAR_VIEW) => any;
